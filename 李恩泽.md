# 广告制作流程梳理

## 一、标准的广告制作流程

1. 需求 Brief
   - 先明确目标市场、投放平台、受众画像、核心卖点、禁忌项、视频时长、语言要求和转化目标。

2. 创意策略
   - 确定广告类型：促销、品牌故事、拉新、测评、种草等。
   - 明确创意方向：市场优先、文案保真、导演强化。

3. 脚本与分镜
   - 将文案拆成开场钩子、卖点展开、证据、情绪转折、行动号召。
   - 每一段分镜都要有对应的镜头说明和时长。

4. 台词与字幕
   - 台词按句编辑，字幕与口播逐句对齐。
   - 支持单语或双语字幕，必要时区分投放市场版本。

5. 视觉与模型配置
   - 选择画幅、风格、运镜、素材来源、图片参考、是否启用原生音频。

6. 生成与试版
   - 先生成首版，再做 A/B 版本对比。
   - 根据失败原因自动切换备用模型或重新提交。

7. 审核与修正
   - 检查市场是否跑偏、字幕是否对齐、台词是否准确、品牌信息是否被过度改写。

8. 导出与投放
   - 输出成片、投放包、对比报告和复盘建议。

## 二、当前流程缺少什么

- 缺少独立的广告 Brief 入口，市场、语言、禁忌项、CTA 没有成为一等输入。
- 缺少广告专用编辑工作台，容易跳到旧的通用项目创建页。
- 缺少逐句台词编辑区，字幕和口播容易脱节。
- 缺少草稿自动保存和恢复，用户一旦跳页就丢上下文。
- 缺少面向广告的审核清单和版本对比机制。
- 缺少把“导演调整”约束在市场范围内的规则，容易把内容改成泛化版本。

## 三、制作过程中遇到的问题和难点

1. 市场不对
   - 生成结果容易变成通用口吻，失去目标市场的语气和消费场景。

2. 字幕和台词不一致
   - 没有逐句绑定时，视频中说的内容和字幕会出现偏差，影响可信度和转化。

3. 过度依赖旧流程
   - 旧项目创建页更偏通用项目，不适合广告制作的连续编辑和快速修正。

4. 交互链路太长
   - 用户需要在多个页面之间切换，不能边看边改，编辑效率低。

5. 模型能力不一致
   - 有的模型支持原生音频，有的模型只适合无声视频，参数不能统一处理。

6. 反复重试成本高
   - 一旦失败，若缺少上下文保存，重新提交就要重新配置所有参数。

## 四、解决方案

### 1. 独立广告工作台
- 把广告制作从通用项目创建中拆出来，形成独立的广告页工作流。
- 广告页内直接承载文案、市场、字幕、导演备注和模型选择。

### 2. 把市场与字幕作为一等输入
- 增加目标市场、字幕语言、创意模式、导演备注等控制项。
- 文案优化时把这些约束一起送入提示词，而不是只做语言润色。

### 3. 台词与字幕逐句对齐
- 将字幕文本拆分成逐句台词，按镜头数量分发到每一段分镜。
- 在生成请求里同时传 `subtitle_text`、`dialogues`、`subtitle_style` 和 `render_config`。

### 4. 草稿自动保存与恢复
- 广告页不再跳到旧创建页，而是把当前编辑状态保存为本地草稿。
- 用户可以在当前页面持续修改，不会因为切页丢失上下文。

### 5. 引入复投与对比
- 支持自动切备用模型重试。
- 支持 A/B 版本批量提交、结果对比和投放建议导出。

### 6. 约束导演调整范围
- 把“导演强化”限制在市场和品牌约束内，防止自动改写把核心卖点冲掉。

## 五、当前制作状态

- 当前先在本地完善广告流程，不主动推送到远端；需要同步时再单独说明。
- 已把广告页从旧项目创建页中解耦出来。
- 已补充市场、字幕语言、创意模式和导演备注输入。
- 已把字幕/台词输入接入生成请求和后端处理。
- 已实现本地草稿自动保存和恢复。
- 已增加本地审核清单和分镜可视化编辑，生成前需要先勾选确认。
- 已增加分镜模板库和镜头参考图选择提示，分镜卡片可以直接套用模板并填写参考图说明。
- 已增加品牌语气模板和模板切换对比预览，品牌语气会写入文案优化和视频生成提示词。
- 已增加本地版本记录，支持保存当前版本、历史版本对比和一键恢复到当前页面。
- 下一步可以继续做：
   - 增加模板对比统计；
   - 增加更细的品牌禁用词和行业词库。

## 六、需求与对应开发

1. 需求：广告流程必须留在当前页面，不能跳回旧项目创建页。
   - 开发：广告页保持独立工作台，生成后也不自动跳转，草稿和编辑状态继续留在当前页。

2. 需求：市场、字幕语言、创意模式、导演备注要前置。
   - 开发：在表单中增加目标市场、字幕语言、创意模式和导演备注，并把这些信息进入提示词和 render_config。

3. 需求：台词和字幕必须对齐。
   - 开发：将字幕文本拆成 dialogues，生成请求同时传 subtitle_text 和 dialogues，后端保留 render_config.dialogues。

4. 需求：支持连续编辑和多次试版。
   - 开发：提供 localStorage 草稿恢复、自动保存、A/B 版本重试和备用模型切换。

5. 需求：市场不能被导演调整冲掉。
   - 开发：在文案优化和生成约束中明确市场边界，限制改写范围，让导演调整只做局部强化。

6. 需求：后续要继续扩展分镜和审核能力。
   - 开发：当前已实现基础审核清单、分镜可视化编辑、分镜模板库、品牌语气模板和本地版本记录，后续继续扩展模板对比统计、版本对比和投放复盘入口。

## 七、结论

标准广告制作不应该是“先生成，再修补”的通用流程，而应该是“市场 Brief → 文案/台词 → 分镜 → 生成 → 审核 → 投放”的可控流程。当前项目最核心的改造方向是：把市场、字幕和导演备注前置，把旧项目创建页降级为通用项目入口，让广告页成为真正可持续编辑的独立工作台。